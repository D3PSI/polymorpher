#include <stdio.h>

/*
 * The main entry point for the polymorph code.
 * The idea is to modify the executable at runtime
 * and shuffle bare assembly blocks around.
 */
int main() {

    return 0;
}
